class HomeController < ApplicationController; layout false
  # class HomeController < ApplicationController; layout false
  # is an optional class that makes prevents the application controller
  def index
  end
end
