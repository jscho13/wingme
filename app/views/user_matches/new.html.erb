<div class="row text-center">
  <h1>Match Friends</h1>
  <% current_user.minus_friend(@selected_user).each do |user| %>
    <%= link_to "#{user.name}", user_matches_path(@selected_user, matched_user: user.id), :method => :post %>
  <% end %>
</div>
