<h1>Match Friends</h1>
<% @friends.each_with_index do |user, index| %>
  <%= link_to "#{user.first_name} #{user.last_name}", user_matches_path(@matched_user, user_to_match: user.id), :method => :post %>
<% end %>
